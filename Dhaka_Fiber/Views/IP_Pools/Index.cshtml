@model IEnumerable<Dhaka_Fiber.Models.IP_Pools>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Pool_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IP_Range)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Router)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Reamaks)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Pool_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IP_Range)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Router)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Reamaks)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
